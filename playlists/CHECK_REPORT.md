# India playlist — link check report

_Last refreshed automatically: **2026-08-10 20:43 UTC**_

Generated by the weekly `refresh` GitHub Action. Each stream is fetched and, for HLS endpoints, verified to return a valid `#EXTM3U` manifest.

> **Point-in-time snapshot.** Live IPTV streams flap; the list is re-verified every week.

## Sources

Hand-curated list plus the [iptv-org](https://github.com/iptv-org/iptv) India (`countries/in`) and Hindi, Tamil, Telugu, Bengali & Malayalam (`languages/*`) collections, deduplicated by URL.

## Results

| Bucket | Count | File |
|--------|-------|------|
| ✅ Active — HTTPS (browser + player safe) | 549 | `india-active.m3u` |
| ✅ Active — HTTP (native players only) | 40 | `india-active-http.m3u` |
| ❌ Dead / unreachable | 304 | — |
| Source pool (all) | 893 | `india.m3u` |

`india-active.m3u` powers the web set-top box (HTTPS only, so it is safe to play from the `https://` GitHub Pages site). `http://` streams cannot play on the HTTPS page (mixed content) — use `india-active-http.m3u` in a native player like VLC.

## Notes on dead links

Most failures are genuine `404`/`403`. Some `403`s are **geo-blocking** — channels that reject non-India IPs — so they may still play for you locally.
